.application-view

	.application-view-header

		.application-view-header-label

			h2.application-view-header-label__title
				| Add a sighting

		// </label>

	// </header>

	.application-view-main

		.utility.extent.inset
			
			form.form-sighting(autocomplete="off",data-ng-submit="Form.record('name', '#/form/location');")

				.form-sighting-main

					ul.form-sighting-options

						li.form-sighting-options-enclosure

							.form-sighting-option
								
								.form-sighting-option-label

									label.form-sighting-option-label__title(for="sighting-name")
										| Pokemon

								// </label>
								
								.form-sighting-option-media

									.autocomplete

										.autocomplete-option
										
											input.form-sighting__input.field#sighting-name(data-ng-model="Form.formData.name",placeholder="Pokemon name...",type="text")
										
										.autocomplete-result(data-ng-show="Form.formData.name.length && filtered.length")
											
											ul.autocomplete-result-list
												
												li.autocomplete-result-list-enclosure(data-ng-repeat="poker in Form.pokemon | filter: {Name: Form.formData.name} as filtered")
													
													a.card-preview(data-ng-click="Form.autocomplete('name',poker.Name)")

														.card-preview-media

															img.card-preview-media__object(height="26",data-ng-src="/api/pokemon/icons/{{poker.Number}}.ico",width="26")

														// </media>

														.card-preview-label

															span.card-preview-label__title
																| {{poker.Name}}

														// </label>

													// </card>

												// </enclosure>

											// </list>

										// </result>
									
									// </autocomplete>

								// </media>
							
							// </option>
						
						// </enclosure>
					
					// </options>
				
				// </main>
			
			// </form>
		
		// </extent>
	
	// </main>

	.application-view-footer(data-ng-class="{'active':Form.formData.name}")

		button.button-outline.utility.foreground-color-white(data-ng-click="Form.record('name', '#/form/location');")

			.button-outline-label

				span.button-outline-label__title
					| Add location

			// </label>

		// </outline>

	// </footer>

// </view>